apiVersion: apps/v1
kind: Deployment
metadata:
  name: seatsolver
  namespace: east1
  labels:
    app: seatsolver
    environment: production
    region: east1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: seatsolver
  template:
    metadata:
      labels:
        app: seatsolver
    spec:
      containers:
      - name: seatsolver
        image: myregistry/seatsolver:v2.0
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: REGION
          value: "east1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: seatsolver-service
  namespace: east1
spec:
  selector:
    app: seatsolver
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
# Test configuration added via UI
# Demo: Auto-sync working perfectly!