apiVersion: apps/v1
kind: Deployment
metadata:
  name: demoservice
  namespace: east1
  labels:
    app: demoservice
    environment: production
    region: east1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demoservice
  template:
    metadata:
      labels:
        app: demoservice
    spec:
      containers:
      - name: demoservice
        image: myregistry/demoservice:v1.0
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: REGION
          value: "east1"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: demoservice-service
  namespace: east1
spec:
  selector:
    app: demoservice
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP

# Added via automated test