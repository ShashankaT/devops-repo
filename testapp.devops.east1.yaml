apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp
  namespace: east1
  labels:
    app: testapp
    environment: production
    region: east1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: testapp
  template:
    metadata:
      labels:
        app: testapp
    spec:
      containers:
      - name: testapp
        image: myregistry/testapp:v1.0
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: REGION
          value: "east1"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: testapp-service
  namespace: east1
spec:
  selector:
    app: testapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP

# Auto-sync test